#Given the length and width of a rectangle,
#compute both its area and perimeter
#using arithmetic operators

length = 3
width = 4

area = (length * width)
perimeter = 2 * (int(length + width))
print(area, perimeter)
